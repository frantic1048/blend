# Blend website

The source for Blend's single-page project website lives here. It is a static
Astro site with no client-side JavaScript or external styling framework.

## Development

From this directory:

```sh
pnpm install
pnpm dev
```

Before submitting changes, run:

```sh
pnpm check
pnpm build
```

The production output is written to `dist/`.
